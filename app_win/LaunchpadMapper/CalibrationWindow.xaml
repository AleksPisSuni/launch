<Window x:Class="LaunchpadMapper.CalibrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calibrate Launchpad" Height="240" Width="420" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Calibration (3-point)" FontWeight="Bold" FontSize="14"/>
        <TextBlock x:Name="TxtInstruction" Grid.Row="1" Margin="0,8,0,0" TextWrapping="Wrap">Press the bottom-left pad on your Launchpad now.</TextBlock>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="Bottom-left:" FontWeight="Bold"/>
            <TextBlock x:Name="TxtBL" Margin="8,0,0,0">—</TextBlock>
            <TextBlock Text="   Bottom-right:" FontWeight="Bold" Margin="16,0,0,0"/>
            <TextBlock x:Name="TxtBR" Margin="8,0,0,0">—</TextBlock>
            <TextBlock Text="   Top-left:" FontWeight="Bold" Margin="16,0,0,0"/>
            <TextBlock x:Name="TxtTL" Margin="8,0,0,0">—</TextBlock>
        </StackPanel>

        <TextBox x:Name="TxtLog" Grid.Row="3" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnCancel" Width="100" Click="BtnCancel_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
